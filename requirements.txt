# Prefer installing from backend/ for the API:
#   pip install -r backend/requirements.txt
#
# This root file delegates so `pip install -r requirements.txt` stays in sync.
-r backend/requirements.txt
